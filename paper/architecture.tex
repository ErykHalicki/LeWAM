
\definecolor{cblue}{HTML}{4A90D9}
\definecolor{cgreen}{HTML}{5BAD72}
\definecolor{corange}{HTML}{E8863A}
\definecolor{cpurple}{HTML}{9B6BBE}
\definecolor{cgray}{HTML}{7F8C8D}
\definecolor{cred}{HTML}{D9534F}

\begin{tikzpicture}[
    module/.style={draw, thick, rounded corners=4pt, minimum height=3em,
                   align=center, font=\footnotesize\sffamily\bfseries},
    enc/.style={module, fill=cblue!12, draw=cblue!50!black, text=cblue!60!black,
                minimum width=7em},
    lbl/.style={font=\footnotesize\sffamily},
    sublbl/.style={font=\scriptsize\sffamily, text=cgray!80!black},
    arr/.style={-{Stealth[length=4pt, width=3pt]}, semithick, color=black!60},
    op/.style={draw, thick, circle, minimum size=1.8em, inner sep=0pt,
               font=\small\sffamily\bfseries, fill=white},
    imgframe/.style={inner sep=1pt, draw=cgray!30, rounded corners=1pt},
]

%% ── Camera 0 stack (top) ──
\coordinate (c0base) at (0, 0.9);
\foreach \i/\file in {0/cam0_ctx_f0, 1/cam0_ctx_f8, 2/cam0_ctx_f15} {
    \node[imgframe] at ($(c0base)+(\i*2pt, -\i*2pt)$)
        (c0f\i) {\includegraphics[width=0.85cm]{figures/generated/\file.png}};
}
\node[sublbl, left=2pt of c0f0.west, anchor=east] {Cam 0};

%% ── Camera 1 stack (bottom) ──
\coordinate (c1base) at (0, -0.9);
\foreach \i/\file in {0/cam1_ctx_f0, 1/cam1_ctx_f8, 2/cam1_ctx_f15} {
    \node[imgframe] at ($(c1base)+(\i*2pt, -\i*2pt)$)
        (c1f\i) {\includegraphics[width=0.85cm]{figures/generated/\file.png}};
}
\node[sublbl, left=2pt of c1f0.west, anchor=east] {Cam 1};

%% ── Stitch operator ──
\node[op] at (2.2, 0) (stitch) {$\oplus$};
\node[sublbl, above=2pt of stitch] {stitch};

\draw[arr] (c0f2.east) -- ++(0.3, 0) |- (stitch.west);
\draw[arr] (c1f2.east) -- ++(0.3, 0) |- (stitch.west);

%% ── V-JEPA2 ──
\node[enc, right=1.2cm of stitch, minimum width=7em] (vjepa) {V-JEPA2\\[-2pt]{\scriptsize(frozen)}};
\draw[arr] (stitch) -- (vjepa);

\end{tikzpicture}
